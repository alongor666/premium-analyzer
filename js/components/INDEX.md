# /js/components/ - UI组件层

> **可复用UI组件**

最后更新: 2026-01-04

---

## 🎯 目录职责

本目录包含应用的所有UI组件,采用组件化设计模式,每个组件负责独立的功能模块。所有组件通过EventBus通信,避免直接引用,确保组件解耦和可复用性。

---

## 🔗 关键入口

### 主要文件
- [file-uploader.js](file-uploader.js) - 文件上传组件
  - 拖拽上传支持
  - Excel/CSV解析(PapaParse + SheetJS)
  - 文件验证和错误处理
  - 上传进度显示

- [dimension-selector.js](dimension-selector.js) - 维度选择器
  - 电商式下拉筛选
  - draft → applied 状态模式
  - 多维度联动
  - 搜索和排序功能

- [metric-card.js](metric-card.js) - 指标卡片
  - KPI指标显示
  - 动态数值更新
  - 响应式布局

### 测试文件
- 无(当前无单元测试)

---

## 📚 全局索引链接
- 📖 [文档索引](../../docs/00_index/DOC_INDEX.md)
- 💻 [代码索引](../../docs/00_index/CODE_INDEX.md)
- 📊 [进展索引](../../docs/00_index/PROGRESS_INDEX.md)

---

## ⚠️ 约束条件

### 禁止修改
- 🚫 **禁止修改**: 组件核心逻辑(已通过安全审计)
- 🚫 **禁止修改**: 事件通信机制
- 🚫 **禁止修改**: draft → applied 状态模式

### 允许操作
- ✅ **允许操作**: 添加新组件
- ✅ **允许操作**: 调整组件样式
- ✅ **允许操作**: 扩展组件功能

### 设计原则
- **单一职责**: 每个组件只负责一个UI功能
- **事件驱动**: 组件通过EventBus通信
- **配置驱动**: UI通过JSON配置生成
- **响应式**: 支持不同屏幕尺寸

---

## 📊 代码统计

| 文件 | 估计行数 | 状态 | 复杂度 |
|------|----------|------|--------|
| file-uploader.js | ~400 | ✅ 稳定 | 中 |
| dimension-selector.js | ~600 | ✅ 稳定 | 高 |
| metric-card.js | ~200 | ✅ 稳定 | 低 |

---

## 🎯 使用指南

### FileUploader使用示例
```javascript
const uploader = new FileUploader({
  container: '#upload-section',
  onFileLoaded: (data) => {
    EventBus.emit('data-loaded', { rows: data });
  }
});
```

### DimensionSelector使用示例
```javascript
const selector = new DimensionSelector({
  container: '#filter-container',
  dimensions: dimensionsConfig,
  onFilterChange: (filters) => {
    // 更新draft状态
    StateManager.setDraftFilters(filters);
  }
});
```

### MetricCard使用示例
```javascript
const card = new MetricCard({
  container: '#metric-cards',
  title: '总保费',
  value: 1234567,
  format: 'currency'
});
```

---

## 📞 联系信息
- **负责人**: 前端开发团队
- **更新频率**: 按需(新功能添加时)
- **相关文档**: [CLAUDE.md](../../CLAUDE.md) - 组件通信模式

---

*维护者: 开发团队*
*最后更新: 2026-01-04*
